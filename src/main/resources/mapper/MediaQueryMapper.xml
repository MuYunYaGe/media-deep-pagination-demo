<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mediapagination.infrastructure.mysql.MediaQueryMapper">

    <resultMap id="mediaRow" type="com.example.mediapagination.infrastructure.mysql.MediaRow">
        <id property="id" column="id"/>
        <result property="categoryId" column="category_id"/>
        <result property="title" column="title"/>
        <result property="publishTime" column="publish_time" javaType="java.time.Instant"/>
        <result property="status" column="status" javaType="com.example.mediapagination.domain.MediaStatus"/>
        <result property="createdAt" column="created_at" javaType="java.time.Instant"/>
        <result property="updatedAt" column="updated_at" javaType="java.time.Instant"/>
    </resultMap>

    <resultMap id="indexEntry" type="com.example.mediapagination.application.model.MediaIndexEntry">
        <constructor>
            <idArg column="id" javaType="long"/>
            <arg column="publish_time" javaType="java.time.Instant"/>
        </constructor>
    </resultMap>

    <sql id="mediaColumns">
        id, category_id, title, publish_time, status, created_at, updated_at
    </sql>

    <select id="findPublishedPage" resultMap="mediaRow">
        SELECT <include refid="mediaColumns"/>
        FROM media
        WHERE category_id = #{categoryId}
          AND status = 'PUBLISHED'
        ORDER BY publish_time DESC, id DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countPublished" resultType="long">
        SELECT COUNT(*)
        FROM media
        WHERE category_id = #{categoryId}
          AND status = 'PUBLISHED'
    </select>

    <select id="findPublishedByIds" resultMap="mediaRow">
        SELECT <include refid="mediaColumns"/>
        FROM media
        WHERE status = 'PUBLISHED'
          AND id IN
          <foreach collection="ids" item="id" open="(" separator="," close=")">
              #{id}
          </foreach>
    </select>

    <select id="findById" resultMap="mediaRow">
        SELECT <include refid="mediaColumns"/>
        FROM media
        WHERE id = #{id}
    </select>

    <select id="findPublishedIndexBatch" resultMap="indexEntry">
        SELECT id, publish_time
        FROM media
        WHERE category_id = #{categoryId}
          AND status = 'PUBLISHED'
        <if test="beforeTime != null">
          AND (
              publish_time &lt; #{beforeTime}
              OR (publish_time = #{beforeTime} AND id &lt; #{beforeId})
          )
        </if>
        ORDER BY publish_time DESC, id DESC
        LIMIT #{limit}
    </select>

</mapper>
